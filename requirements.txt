# Pinned to a known-stable combo. Pandas 3.0 (released April 2026) breaks
# statsmodels/patsy; Python 3.14 is too new for some scientific libs.
# These pins keep the app reproducible across local + cloud installs.

streamlit>=1.30,<2.0
pandas>=2.1,<3.0
numpy>=1.24,<2.5
openpyxl>=3.1
scipy>=1.11,<1.16
statsmodels>=0.14,<0.15
matplotlib>=3.7,<4.0
python-docx>=1.1
PyYAML>=6.0
